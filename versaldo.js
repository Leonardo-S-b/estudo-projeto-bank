const prompt = require("prompt-sync")();

function versaldo(saldo, historico){
    console.log(`Seu saldo é de ${saldo.toFixed(2)}.`)  //declarações de funções bem facil até//
}
module.exports = versaldo